# Sentinel Backend — Dependencies

# Core
fastapi==0.115.12
uvicorn[standard]==0.34.3
pydantic>=2.0

# Data Processing
openpyxl>=3.1.0
rapidfuzz>=3.0.0

# AI / ML
sentence-transformers>=3.0.0
numpy>=1.24.0

# HTTP client (used by OllamaProvider and future async needs)
httpx>=0.27.0

# LLM provider SDKs (install the one(s) you need)
google-genai>=1.0.0   # required when LLM_PROVIDER=gemini
# openai>=1.0.0       # required when LLM_PROVIDER=openai

# Utilities
python-dotenv>=1.0.0
